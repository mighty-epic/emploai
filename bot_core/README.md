# Compatibility Shim

This folder only preserves the old `bot_core.*` import path.

Active shared support code lives in `runtime_support/`. Do not add new runtime
code here; update the active package and keep this shim minimal.
