# Legacy Web App

This is an older standalone FastAPI/static-web prototype. It is preserved for
reference, but the current EmploAI product path is the Electron desktop app in
`desktop_app/` backed by `app_backend/`.

Do not add new desktop UI work here. Use `desktop_app/renderer_client/` for UI
changes and `app_backend/` for the local app API.
