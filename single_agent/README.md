# Compatibility Shim

This folder only preserves the old `single_agent.*` import path.

Active local agent code lives in `local_agent_runtime/`. Do not add new runtime
code here; update the active package and keep this shim minimal.
