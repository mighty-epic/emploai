# Compatibility Shim

This folder only preserves the old `agent.*` import path.

Active code lives in `legacy_agent_orchestration/`. Do not add new runtime code
here; update the active package and keep this shim minimal.
