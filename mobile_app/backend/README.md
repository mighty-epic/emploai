# Compatibility Shim

This folder only preserves the old `mobile_app.backend.*` import path.

Active local app backend code lives in `app_backend/`. Do not add new backend
code here; update the active package and keep this shim minimal.
